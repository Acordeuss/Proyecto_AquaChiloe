spring:
  application:
    name: ms-gateway

server:
  port: ${PORT:8090}

services:
  centros: ${CENTROS_URL:http://localhost:8080}
  biomasa: ${BIOMASA_URL:http://localhost:8081}
  lotes: ${LOTES_URL:http://localhost:8082}
  alimentacion: ${ALIMENTACION_URL:http://localhost:8083}
  ambiental: ${AMBIENTAL_URL:http://localhost:8084}
  sanidad: ${SANIDAD_URL:http://localhost:8085}
  personal: ${PERSONAL_URL:http://localhost:8086}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
